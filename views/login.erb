<%if !session[:userID].empty? && !session[:userName].empty? then %>
  <script>
    window.location.href = "/";
  </script>
<% end %>

<div class="container top_adjust">
  <div class="col-xs-8 col-md-offset-2">
    <h1 class="text-center">ログインページ</h1>
    <form action="/" method="post">
        <div class="form-group-lg">
          <label class="h2">  userID</label>
            <input type="text" name="userID" style="ime-mode: disabled" class="form-control">
        </div>
        <div class="form-group-lg">
          <label class="h2"> password</label>
          <input type="password" name="password" class="form-control"placeholder="passwordは形だけ">
        </div>

      <div class="btn-toolbar">
          <button class="btn btn-default"name ="login">log in</button>
    </form>
      <form action="/registration" method="get">
          <button id= "registration" class="btn btn-default">registration</button>
      </form>
      </div>
  </div>
  <!-- <div class="col-xs-2"></div> -->
</div>
<script>
 $(function(){
  $('#registration').on("click",function(){ location.href="/registration";});
});
</script>
